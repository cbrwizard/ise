const React = require('react-native');
const {StyleSheet} = React;

const textParagraphDefaultStyle = StyleSheet.create({
  paragraph: {
    fontSize: 14,
    color: '#D2D2D2',
    marginBottom: 5,
  }
});

module.exports = textParagraphDefaultStyle;
