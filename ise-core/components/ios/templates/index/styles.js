const React = require('react-native');
const {StyleSheet} = React;

const templatesIndexDefaultStyle = StyleSheet.create({
  view: {
    flex: 1,
    backgroundColor: '#3C3C3C',
    paddingTop: 25,
    paddingBottom: 25,
  }
});

module.exports = templatesIndexDefaultStyle;
